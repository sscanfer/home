#include <stdio.h>



int main(int argc, char **argv)
{
    // 输入字符串：最长12位，表示6个字节

    // 判断是否合法
        // 对于单字节
            // 第一位为0
        // 对于多字节字符
            // 第一个字节是否合法：前 n 个位都是1，第 n + 1 位为 0
            // 后续字节是否合法： 以 10 开头

    // 如果合法，计算编码值

    return 0;
}